#include <iostream>
#include <cmath>
using namespace std;
int main()
{
  int n, flag=1;
  cin>>n;

  for(int i=2; i<=sqrt(n); i++)
    if(n%i == 0){
      flag=0;
      break;
    }
  flag ? cout<<"Prime Number"<<endl : cout<<"Not Prime Number"<<endl;
  return 0;
}
